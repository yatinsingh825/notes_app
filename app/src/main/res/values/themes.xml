<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light Theme -->
    <style name="Theme.Notes_app" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/my_primary</item>
        <item name="android:colorPrimaryDark">@color/my_primary</item>
        <item name="android:colorAccent">@color/teal_200</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/my_primary</item>
    </style>

    <!-- Dark Theme -->
    <style name="Theme.Notes_app.Dark" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/my_primary</item>
        <item name="android:colorPrimaryDark">@color/my_primary</item>
        <item name="android:colorAccent">@color/teal_700</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <item name="android:background">@color/dark_background</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>